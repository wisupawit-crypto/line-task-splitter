{
  "name": "line-task-splitter-bot",
  "version": "0.1.0",
  "private": true,
  "description": "LINE bot ที่เก็บงานจากกลุ่มแล้วส่งไปหาแต่ละคน",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vercel": "^37.0.0"
  }
}
