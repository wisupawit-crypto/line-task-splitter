# คู่มือติดตั้งบอท LINE Task Splitter

ใช้เวลาประมาณ **40–60 นาที** ถ้าไม่เคยทำมาก่อน ทุกอย่างในนี้ฟรีหมด ยกเว้นค่า LLM ที่คิดตามจริง (หลักสิบบาทต่อเดือนสำหรับร้านเดียว)

---

## 📁 โครงไฟล์ที่ต้องได้

ไฟล์ที่ผมสร้างให้เป็นไฟล์แบน ๆ ต้องจัดเข้าโฟลเดอร์ตามนี้ก่อน

```
line-task-splitter/
├── index.html              ← prototype เดิม (มีอยู่แล้วใน repo)
├── package.json
├── tsconfig.json
├── vercel.json
├── .env.example
├── api/
│   ├── webhook.ts          ← จาก webhook.ts
│   └── daily-digest.ts     ← จาก daily-digest.ts
├── lib/
│   ├── line.ts             ← จาก line.ts
│   ├── parse.ts            ← จาก parse.ts
│   ├── llm.ts              ← จาก llm.ts
│   └── db.ts               ← จาก db.ts
├── test/
│   └── parse.test.ts       ← จาก parse.test.ts
└── supabase/
    └── schema.sql          ← จาก schema.sql
```

---

## 1️⃣ Supabase (ฐานข้อมูล)

1. สมัคร/เข้า https://supabase.com → **New project**
   - Region เลือก **Southeast Asia (Singapore)** จะเร็วที่สุดสำหรับไทย
   - ตั้ง database password แล้วเก็บไว้ให้ดี
2. รอสร้างเสร็จ (~2 นาที) → เมนูซ้าย **SQL Editor** → **New query**
3. คัดลอกทั้งไฟล์ `supabase/schema.sql` ไปวาง แล้วกด **Run**
4. ไป **Project Settings → API** เก็บสองค่านี้ไว้
   - `Project URL` → `SUPABASE_URL`
   - `service_role` secret → `SUPABASE_SERVICE_ROLE_KEY`

> ⚠️ `service_role` key ข้าม RLS ได้ทั้งหมด ใช้เฉพาะฝั่ง server เท่านั้น ห้ามใส่ในโค้ดที่รันบนเบราว์เซอร์ และห้าม commit ขึ้น git

---

## 2️⃣ LINE Official Account + Messaging API

1. เข้า https://developers.line.biz/console/ ล็อกอินด้วย LINE ส่วนตัว
2. **Create a new provider** (ใส่ชื่อร้านได้เลย)
3. ในโปรไวเดอร์นั้น → **Create a Messaging API channel**
   - ชื่อ channel = ชื่อบอทที่จะโชว์ในกลุ่ม
   - Category เลือกให้ตรงกับธุรกิจ
4. แท็บ **Basic settings** → เก็บ `Channel secret` → `LINE_CHANNEL_SECRET`
5. แท็บ **Messaging API** → ล่างสุด `Channel access token (long-lived)` กด **Issue** → `LINE_CHANNEL_ACCESS_TOKEN`

### ตั้งค่าที่ "ต้องทำ" ไม่งั้นบอทใช้ในกลุ่มไม่ได้

ในแท็บ **Messaging API**:

| การตั้งค่า | ต้องเป็น | เหตุผล |
|---|---|---|
| Allow bot to join group chats | **Enabled** | ไม่งั้นเชิญเข้ากลุ่มไม่ได้ |
| Auto-reply messages | **Disabled** | ไม่งั้นบอทตอบข้อความอัตโนมัติซ้อนกัน |
| Greeting messages | **Disabled** | เหมือนกัน |
| Webhook | **Enabled** | หัวใจของระบบ |

> การปิด auto-reply ต้องไปทำใน **LINE Official Account Manager** (มีลิงก์จากหน้านั้น) → Settings → Response settings

---

## 3️⃣ Anthropic API key (ชั้นสะกิดถาม)

1. เข้า https://console.anthropic.com → **API Keys** → สร้าง key ใหม่
2. เก็บไว้เป็น `ANTHROPIC_API_KEY`

> ข้ามขั้นนี้ได้ ถ้าเว้นว่างไว้ บอทจะทำงานเฉพาะโหมดคำสั่ง `/งาน` — ยังใช้งานได้ปกติ แค่ไม่มีตัวสะกิดเตือนตอนลืมใช้คำสั่ง

**ค่าใช้จ่ายคร่าว ๆ:** โค้ดกรองข้อความก่อนยิง LLM อยู่แล้ว (ฟังก์ชัน `worthAsking`) ข้อความส่วนใหญ่ในกลุ่มจะไม่ถูกส่งไปถาม ร้านที่คุยกันวันละ 200 ข้อความ จะมีที่เข้าเงื่อนไขจริงราว 20–30 ข้อความ ตกเดือนละไม่กี่สิบบาท

---

## 4️⃣ Deploy ขึ้น Vercel

1. เข้า https://vercel.com → **Add New → Project**
2. เลือก repo `line-task-splitter` → **Import**
3. **Environment Variables** ใส่ให้ครบก่อนกด Deploy:

```
LINE_CHANNEL_SECRET
LINE_CHANNEL_ACCESS_TOKEN
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
ANTHROPIC_API_KEY        (ใส่หรือไม่ใส่ก็ได้)
LLM_MODEL                claude-haiku-4-5-20251001
CRON_SECRET              สุ่มอะไรก็ได้ที่เดายาก
```

4. **Deploy** → ได้ URL มาประมาณ `https://line-task-splitter-xxxx.vercel.app`

### ผูก Webhook กลับไปที่ LINE

กลับไปที่ LINE Developers → แท็บ **Messaging API** → **Webhook URL** ใส่:

```
https://<โดเมนที่ Vercel ให้มา>/api/webhook
```

กด **Update** → กด **Verify** ต้องขึ้น **Success** สีเขียว

> ถ้าขึ้น error 401 = `LINE_CHANNEL_SECRET` ไม่ตรง
> ถ้าขึ้น 500 = ดู log ที่ Vercel → Deployments → Functions

---

## 5️⃣ ทดสอบใช้งานจริง

1. แท็บ **Messaging API** → สแกน QR code เพื่อ **แอดบอทเป็นเพื่อน**
   (ขั้นนี้สำคัญมาก — ทุกคนในทีมต้องทำ ไม่งั้นบอท DM หาไม่ได้)
2. สร้างกลุ่ม LINE ทดสอบ → เชิญบอทเข้ากลุ่ม → บอทจะทักทายพร้อมบอกวิธีใช้
3. ลองพิมพ์:

```
/งาน @ตัวเอง เช็คสต็อกแก้ว ก่อนบ่าย 3
```

ควรได้: บอทตอบยืนยันในกลุ่ม + การ์ดงานเด้งเข้าแชทส่วนตัวพร้อมปุ่ม ✅

4. ลองพิมพ์แบบไม่ใช้คำสั่ง เช่น `เดี๋ยวช่วยเช็คของหน่อยนะ` → บอทควรสะกิดถามพร้อมปุ่มยืนยัน
5. พิมพ์ `/งานค้าง` เพื่อดูงานทั้งหมดในกลุ่ม

---

## 🔁 คำสั่งทั้งหมด

| ที่ไหน | พิมพ์ | ได้อะไร |
|---|---|---|
| ในกลุ่ม | `/งาน @ชื่อ รายละเอียด เวลา` | สร้างงาน + ส่งเข้าแชทส่วนตัวคนนั้น |
| ในกลุ่ม | `/งานค้าง` | ดูงานค้างทั้งหมดแยกตามคน |
| ในกลุ่ม | `/ช่วยเหลือ` | วิธีใช้ |
| แชทส่วนตัว | `งานฉัน` | ดูงานค้างของตัวเอง |

สั่งหลายคนพร้อมกันได้: `/งาน @ฝน @โบ๊ท เติมน้ำเชื่อม ก่อนเปิดร้าน`

**คำบอกเวลาที่ระบบเข้าใจ:** `ก่อนบ่าย 3`, `บ่าย 2 โมง`, `10 โมง`, `5 โมงเย็น`, `2 ทุ่ม`, `14:30`, `ก่อนเที่ยง`, `ตอนปิดร้าน`, `ก่อนเปิดร้าน`, `ภายในวันนี้`, `พรุ่งนี้...`

> เวลาเปิด/ปิดร้านปรับได้ต่อกลุ่ม แก้ที่ตาราง `groups` คอลัมน์ `open_time` / `close_time`

---

## ⏰ สรุปงานอัตโนมัติทุกเช้า

ตั้งไว้แล้วใน `vercel.json` — cron ยิง `/api/daily-digest` ทุกวัน **08:00 น. เวลาไทย** (`0 1 * * *` UTC) แล้วส่งสรุปงานค้างเข้ากลุ่ม พร้อมไฮไลต์งานที่เลยกำหนด

อยากเปลี่ยนเวลา แก้ `schedule` ในไฟล์นั้น (จำไว้ว่าเป็น UTC → เวลาไทยลบ 7)

> Vercel แผนฟรีรัน cron ได้วันละครั้ง ถ้าอยากเตือนถี่กว่านั้นต้องอัปเกรด

---

## 🧪 รันเทสต์

```bash
npm install
npm test        # ทดสอบตัวแกะคำสั่งและตัวแปลงเวลาไทย
npm run typecheck
```

---

## ⚠️ ข้อจำกัดที่ต้องรู้

1. **บอท DM หาคนที่ยังไม่แอดเป็นเพื่อนไม่ได้** — นี่เป็นข้อจำกัดของ LINE เอง แก้ไม่ได้
   ระบบจะบันทึกงานไว้ให้ครบและเตือนในกลุ่มแทน พร้อมบอกว่าใครยังไม่ได้แอด
2. **`@mention` ต้องเลือกจากรายการที่ LINE เด้งขึ้น** — พิมพ์ `@ฝน` เป็นข้อความเปล่า ๆ ไม่นับ เพราะจะไม่มี `userId` แนบมา
3. **บอทอ่านได้เฉพาะข้อความที่พิมพ์หลังจากถูกเชิญเข้ากลุ่ม** — ย้อนหลังไม่ได้
4. **LLM เดาผิดได้** — จึงออกแบบให้ต้องกดยืนยันทุกครั้ง ไม่สร้างงานเองเงียบ ๆ

---

## 🗺 ต่อยอดได้อีก

- เตือนซ้ำเมื่อใกล้ถึงกำหนด / เลยกำหนด
- Dashboard เว็บดูภาระงานรายคน (ต่อ Supabase โดยตรง)
- งานประจำที่วนซ้ำทุกวัน/ทุกสัปดาห์
- สรุปรายสัปดาห์ว่าใครทำเสร็จกี่งาน ตรงเวลาแค่ไหน
- ใช้ตาราง `events` วัดว่า LLM เดาแม่นแค่ไหน (`ai_accepted` เทียบ `ai_rejected`) แล้วปรับ threshold
